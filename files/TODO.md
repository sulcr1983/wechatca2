# SuperSu 任务清单

> 本文件为项目级任务跟踪。每项完成后打 ✅ 并标注完成日期。

## 已完成 ✅

| # | 任务 | 完成日期 | 说明 |
|---|------|----------|------|
| F1 | 小红书封面引擎可用（Playwright + Chromium） | 2026-07-11 | venv 安装 playwright，requirements.txt 标记必需 |
| F2 | 关闭孤儿 LLM 调用 | 2026-07-11 | 默认关闭 _start_background_optimization |
| F3 | 集成测试重写对齐真实前端 | 2026-07-11 | 35/35 全绿 |
| D-1 | 结果卡显示真实封面图（非色块占位） | 2026-07-24 | `<img src="${r.url}">` |
| D-2 | 归藏渲染配色对齐设计铁律 | 2026-07-24 | 局部 tint 替代全画布暗渐变 |
| D-3 | 社交页移动端可见性修复 | 2026-07-24 | 堆叠替代 display:none |
| D-4 | 风格选择器消费 API 规范 id | 2026-07-24 | loadSocialStyles() + data-style |
| W-1 | 公众号移动端主题可访问 | 2026-07-24 | max-height:42vh 可滚动 |
| PM-1 | 公众号页布局重排（模板条+双区） | 2026-07-25 | tpl-bar + wechat-body grid 2-col |
| T1 | 批量生成 84 套颜色克隆主题 | 2026-07-25 | → 后续被删除（用户否决） |
| T2 | 适配 85 套外部主题 | 2026-07-25 | ⚠️ 2026-09-25 已全部废弃并重写为本项目自研 su-*（见 LIC-1） |
| T3 | 去重：删除 84 颜色克隆 + 38 重复 | 2026-07-25 | 安全门限批准后分批删除 |
| T4 | 去重：原创撞色 8 套删除 | 2026-07-25 | 每组留代表，92 套最终 |
| S1 | 小红书页布局重排（2列+画廊+Lightbox） | 2026-07-25 | .social-ctrl/.social-right/.results-grid |
| **S2** | **自动联网搜真图作底图** | **2026-07-25** | **core/image_search.py 双轨 Wikimedia/Pexels** |
| S3 | Playwright file:// → data: URI 修复 | 2026-07-25 | guizang_renderer 底图内嵌 |
| DOC | README 重写（图文并茂+表情丰富） | 2026-07-25 | 4 张截图 + mermaid 架构图 |
| CLEAN | 文档洁癖同步（AGENTS/HANDOFF/design-audit） | 2026-07-25 | 53→92、DOM 结构、搜图模块、偏差标已解决 |
| COPY-1 | 复制按钮写富文本（公众号可粘贴渲染） | 2026-07-26 | ClipboardItem text/html + execCommand 兜底 |
| SEL-1 | 首主题自动选中（输入即渲染） | 2026-07-26 | loadThemes 后 activeTpl=themes[0].id |
| FILT-1 | 模板筛选匹配 name+id+group | 2026-07-26 | 英文 id（如 editorial）也可搜到 |
| E2E-API | 后端 API 全端点 E2E | 2026-07-26 | tests/test_api_e2e.py，29/29 通过 |
| E2E-UI | 前端有头全按钮 E2E（双页全量） | 2026-07-26 | tests/test_headed_full_e2e.py，27/27，0 报错 |
| CLEAN2 | 文档洁癖（AGENTS/HANDOFF 补记本轮） | 2026-07-26 | 复制/首主题/E2E/变更5 |
| E2E-1 | 清理 E2E 陈旧 UI 断言 | 2026-07-26 | test_e2e.py 换真实 UI id，41/41 ✅ |
| D5-FIX | 风格分组对齐 API group | 2026-07-26 | tab 按 /api/social/styles 的 group 动态渲染（3 组） |
| DEADCODE | 删除 render_worker.py 死代码 | 2026-07-26 | git rm，统一走 core/guizang_renderer.py |
| UX-DESIGN | 前端体验走查 + 对抗审计 + 开源选型，方案 v3 定稿 | 2026-09-24 | docs/ux-audit/ 四份文档（含线框原型）；**未动产品代码** |
| P0-BATCH | 第一批止血 U-1…U-6（预览换行/封面溢出/生成进度/署名/示例+记住主题/推送引导） | 2026-09-25 | 提交 a4c869e；回归 52/52·35/35·29/29·33/33·验收15/15 |
| P1-BATCH | 第二批 U-7…U-11（悬停试看/搜索兜底/空态示例/搜图缓存/文案说人话） | 2026-09-25 | 提交 ef5b241；验收 15/15；搜图缓存 25.2s→8.4s |
| U2-FIT | U-2 真自适应（BLCaptain 渲染阶段注入 fitty） | 2026-09-25 | 父仓 48eacf1 + 子仓 6d9e6ed（本地）；33 字长标题自动缩行 |
| PXL-TUT | Pexels 接入（中文 locale+竖版+查询缓存修复）+ 开源教程（按页触发）+ 设置里填 token | 2026-09-25 | 验收 13/13；回归全绿；key 加密存储不入库 |
| ADV-1 | 激活 13 套 `layout:hero` + 1 套 `layout:timeline` 死配置（深色首屏/大序号/交替色带/引文穿插/卡片组/暗色尾屏） | 2026-09-25 | 此前引擎只实现 `card`，这 14 套配置从未被读取 |
| ADV-2 | 新增 4 个模块容器（`:::` 围栏）：`eyebrow` 小标签 / `cards` 卡片组 / `summary` 要点总结 / `cta` 行动引导 | 2026-09-25 | 容器总数 8→12 |
| ADV-3 | 模块样式改为跟随主题（此前除 accent 外全硬编码，92 套主题模块长得一样） | 2026-09-25 | 取色自 `theme.colors` |
| ADV-4 | 前端「插入模块」下拉（11 个模块模板，光标处插入 + 自动渲染） | 2026-09-25 | 有头浏览器目视验收 PASS |
| ADV-FIX1 | 修既有 bug：容器内层 `<p>` 专属样式从未生效（被通用标签注入抢先加 style，精确匹配失配） | 2026-09-25 | HEAD 版本已实测复现 |
| ADV-FIX2 | 修既有 bug：预处理器把容器**内部**短行标成 `##`（`:::stat` 数字、`:::steps` 步骤全废） | 2026-09-25 | `preprocess` 加 container_depth 跟踪 |
| ADV-FIX3 | 修数据丢失风险：容器缺收尾 `:::` 时会把后面整篇当容器内容吞掉 | 2026-09-25 | 改为整段原样输出，绝不吞正文 |
| ADV-FIX4 | **色带可见性**：13 套 hero 主题的交替色带原为 `#fafafa`/`#f7f7f8`（与白底差 15/23，肉眼看不出），改为按 accent 派生可见色带 | 2026-09-25 | 新增 `_resolve_band_bg` + `_BAND_MIN_DISTANCE`(45)；实测色带差 15–50 → 46–57；有头目视确认「明显可见」；回归全绿 |
| ADV-FIX5 | 修文档缺陷：`taskkill //F //IM python.exe` 在本机 PowerShell 报错且失败被 `$null` 吞掉，导致旧进程一直占 5000 端口、改动看起来「没生效」 | 2026-09-25 | AGENTS §4 换成 `Get-Process python \| Stop-Process -Force` 并加端口核验步骤 |
| ADV-FIX6 | 修既有 bug：每个带语言标记的代码块开头多显示一行 `class="language-xxx">`（高亮器把 code 开标签一起正则高亮，标签被拆碎） | 2026-09-25 | `style_pre` 先摘出 code 开标签、高亮后补回 |
| ADV-FIX7 | 修既有 bug：**含 f-string 的代码块会多出 `"color:#ce9178"` 碎片**（高亮器串行正则命中前一步自己插入的 style 属性值） | 2026-09-25 | `_basic_syntax_highlight` 各步片段存入 `\x00H{i}\x00` 占位符，末尾还原 |
| ADV-FIX8 | 修既有 bug：**手写 ```` ``` ```` 围栏被截断**——Python 缩进行被当新代码块，`def` 与 `return` 被拆开、多出空代码块 | 2026-09-25 | `preprocess` 新增 `in_fence` 状态；围栏内原样保留（含缩进）；无围栏纯缩进行仍自动转代码块 |
| LIC-1 | 47 套外部适配主题去授权风险（外部仓库**无 LICENSE = 保留所有权利**，本项目开源=分发） | 2026-09-25 | ✅ 全部重写为本项目自研原创：47 条设计意图（`scripts/theme_specs.py`）→ 本项目设计标尺推导全部数值（`scripts/build_themes.py`）；旧前缀归零、`source` 全清、无重名；旧主题文件与旧适配/去重脚本删除，各文档残留的外部来源表述已清理 |
| LIC-2 | 公开仓不含无授权来源素材 | 2026-09-25 | ✅ `GET /api/themes` 实测 92 套（su- 47 / 旧前缀 0），主题 JSON 无 `source` 字段 |
| ADV-FIX9 | 修生成器 bug：18 套主题的 h2 左边框强调条**静默失效**（值里重复带键名 → `border-left:border-left:…` 被浏览器判非法丢弃） | 2026-09-25 | 有头 E2E 发现；`build_themes.py` 改为 `border_left` + `padding_left` 两个键，并重生成 47 套 |
| ADV-FIX10 | 修 su-deepwater hero 卡片**浅字白底不可读**（卡面写死 `#ffffff`，暗底主题正文是浅色） | 2026-09-25 | 有头 E2E 发现；改为按页面底色向墨色靠 7% 派生卡面，实测 `#0E1726` → 卡面 `#1C2534` |
| ADV-FIX11 | 修 37 套亮底主题代码块**语法高亮对比度过低**（亮底仍用深底调色板，关键字对比度约 2.2:1 发灰） | 2026-09-25 | 有头 E2E 发现；新增 `_SYNTAX_LIGHT` 调色板 + `_is_light_bg()` 按 pre 背景明暗切换 |
| ADV-FIX12 | 修生成器：跳过已存在文件导致改 spec 重跑**静默不生效** | 2026-09-25 | 改为「id 在 SPECS 即本脚本所有，一律覆盖」 |
| ADV-FIX13 | 清死代码：生成器 `fmt()`（未调用）、`surface`（未使用）、原型 `border`/`bullet`/`shadow`（声明未读） | 2026-09-25 | 全部删除；重生成后 44 套输出字节级一致（行为中性） |
| ADV-FIX14 | 修 3 套主题**描述过度承诺**（`su-frostedglass` 白卡、`su-hardedge` 粗黑边+硬阴影、`su-confetti` 多色块均未实现） | 2026-09-25 | 描述改为与实际渲染一致（不实现未声明样式，避免扩大范围） |
| ADV-FIX15 | 修验收脚本：教程弹窗遮罩挡住 `ux_verify_p0/p1` 点击导致**失败**（教程为后加产品行为，两脚本未同步） | 2026-09-25 | 两脚本进小红书页后关闭弹窗；修后 **15/15 + 15/15**；AGENTS §12 计数由失效的「15+13+13」订正为 **15+15+13** |
| BAT-FIX | 修「`start-app.bat` 双击没反应」：① `.bat` 是裸 LF 换行，cmd 解析错乱把行切成半截 ② venv 的 `activate.bat` 写死旧盘符 `d:\test\wechatca2`（项目已在 `e:\`），激活静默失效 → `python` 落到系统解释器 → `No module named 'flask'` | 2026-09-26 | 原地重建 venv 激活脚本（不加 `--clear`，25 个包全保留）；bat 全程改 `%~dp0` 绝对路径调 venv python、不再依赖 activate；新增 `.gitattributes` 锁 `*.bat`/`*.cmd` 为 CRLF；顺带修同病的 `scripts/run_headed_test.bat`（写死旧盘符 + 指向已删文件）。实测：一键启动起服务+开浏览器成功；headed bat 33/33（0 报错）；门禁 52/52 |

## 进行中 / 待办

> 来源：`docs/ux-audit/final-plan.html`（v3 定稿，2026-09-24）。**当前未动代码**，等批准后按序实施；
> 纪律：一次一项 → 改完即测（test_e2e 52/52 + test_integration 35/35，UI 改动加 test_headed_full_e2e 32/32，封面须实跑三引擎出真实 PNG）→ 单项 commit → 回来打勾。

### 第一批 · 止血（P0 必修）

| # | 任务 | 验收（怎么算做完） | 状态 |
|---|------|--------------------|------|
| U-1 | 预览卡 `<br>` 修复（`join('\n')` + CSS 换行） | 预览卡不再出现字面 `<br>` | ✅ 2026-09-25 预览卡已无字面 `<br>`（有头验收 PASS） |
| U-2 | 封面标题溢出：引入 **fitty**（40–76px 自适应），两套引擎模板通用 | 长/短标题 × 3 风格均无溢出、无重叠 | ✅ 2026-09-25 三引擎实跑出图人工核验；BLCaptain sp-mist 溢出已修 |
| U-3 | 生成三阶段进度（找底图→排版→完成）+ 按钮置灰防重复点，**不写假秒数** | 点击后 100ms 内出现进度 | ✅ 2026-09-25 新增 /api/social/progress 真实阶段，前端轮询（验收 PASS） |
| U-4 | 底图署名按实际来源显示（local 时写明降级原因） | 断网跑一次，提示正确 | ✅ 2026-09-25 local 时写明“联网没找到，已用本地底图”（验收 PASS） |
| U-5 | 输入框示例改「看示例 / 清空」+ **记住上次主题** | 重新打开即恢复上次主题 | ✅ 2026-09-25 看示例/清空 + localStorage 记住主题（验收 PASS） |
| U-6 | 推送未配置 → 引导至设置；复制 / 推送**同级醒目** | 未配置点推送有明确指引，不卡空下拉 | ✅ 2026-09-25 未配置→toast 引导 + 打开设置（验收 PASS） |

### 第二批 · 顺手（P1）

| # | 任务 | 验收 | 状态 |
|---|------|------|------|
| U-7 | 悬停即试看 + 最近使用置顶 + 收藏 | 打开到选好常用主题 ≤ 2 步 | ⬜ |
| U-8 | 搜索空结果兜底（清除搜索 / 看最近用的） | 搜不到时有出口 | ⬜ |
| U-9 | 小红书页右侧空态放 3 张示例成品 | 首屏能看到成品样例 | ⬜ |
| U-10 | 搜图结果缓存（**先分段计时**再定并行/复用引擎） | 第二次起耗时明显下降 | ⬜ |
| U-11 | 文案说人话（含去掉假宣传「AI 自动提取标题」） | 界面无术语、无与实际不符的文案 | ⬜ |

### 试点 / 可选（P2）

| # | 任务 | 验收 | 状态 |
|---|------|------|------|
| U-12 | **satori** 单模板双实现对比（视觉一致性 + 耗时） | 达标再谈迁移与去 Chromium | ⬜ |
| U-13 | 风格名中文化 + 模板缩略图 | "静纸·Still Paper" → "素雅" 等 | ⬜ |
| U-14 | 移动端顶栏改短词（公众号 / 小红书 / 设置） | 390px 下单行不换行 | ⬜ |
| U-15 | `output/` 堆积清理策略（当前 366 文件） | 保留最近 N 次或给清理入口 | ⬜ |

### ❌ 明确不做（已否决，勿捡回）

- 首屏常驻「三步走」引导 —— 个人工具无新用户，对老用户是噪音
- 「复制升主按钮、推送降级」 —— 不预设谁更常用；改为同级 + 未配置引导
- 92 套主题人工打标签分类 —— 成本高易打歪；悬停试看 + 置顶已覆盖
- 非技术用户可用性类验收指标 —— 换成「几步 / 几秒 / 几次」的硬指标

### ✅ 已了结 · 开源前法律风险

| # | 任务 | 完成情况 |
|---|------|----------|
| LIC-1 | 47 套 `xh-*` 主题去授权风险（上游 `xiaohu-wechat-format` 无 LICENSE） | ✅ 2026-09-25 全部重写为 `su-*` 自研原创（见「已完成」表） |
| LIC-2 | 公开仓不含无授权来源素材 | ✅ 2026-09-25 `GET /api/themes` 实测 92 套（su- 47 / xh- 0），JSON 无 `source` 字段 |

> ⚠️ 仍留有的历史痕迹（不影响当前主题文件的原创性，属历史记录）：`scripts/remove_dup_themes.py`、
> `docs/task_closure_social-bg.md`、`HANDOFF.md 变更 4` 中仍提及上游仓库名。
> 若要让公开仓彻底看不出沿革，需单独决策是否清理这些历史记录。

---

*最后更新：2026-09-25*
