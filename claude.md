# CLAUDE.md — SuperSu 项目规则（Claude Code 入口）

本文件是 Claude Code 的项目指令入口。项目**唯一宪法真源**为根目录 `AGENTS.md`。

@AGENTS.md

## Claude Code 专属补充
- 本仓库同时被 Codex / WorkBuddy / Claude 使用；所有工程纪律以 `AGENTS.md` 为唯一真源，本文件不重复正文，避免双真源漂移。
- 修改 `AGENTS.md` 后本文件无需改动（通过 `@` 导入自动同步）。
- 测试 / 封面相关验收以 `AGENTS.md §11` 与 `.workbuddy/memory/MEMORY.md` 的计数口径为准（2026-09-23 复核：E2E 52/52、集成 35/35、API 29/29、有头全按钮 32/32、有头用户流程 7/7、有头复制 12/12），互不引用旧计数。
