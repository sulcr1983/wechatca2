# 任务收尾：前端体验诊断 → 对抗审计 → 开源选型 → 方案 v3 定稿

- **日期**：2026-09-24
- **阶段性质**：方案设计（**未改动任何产品代码**，用户明确要求先设计后动手）
- **基线**：git `67411d0`（方案定稿提交）之前的干净工作区

---

## 1. 做了什么

| 轮次 | 内容 | 产物 |
|------|------|------|
| ① 走查 | 有头 Chromium 按真实用户路径走 15 步（粘贴文章→选模板→复制→生成封面→移动端），逐节点截图 + 抓界面文案 | `docs/ux-audit/index.html`、`shots/`×15、`walkthrough.json` |
| ② 审计 | 对 ① 的方案做红队审查：假设结论全错，到源码找反证 | `docs/ux-audit/adversarial-review.html` |
| ③ 选型 | 每个确认问题先搜开源，标注「直接引入 / 抄算法 / 抄交互 / 自研」 | `docs/ux-audit/open-source-picks.html` |
| ④ 定稿 | 融合三轮成果，输出含线框原型的最终方案 | `docs/ux-audit/final-plan.html`（**唯一权威**） |

配套脚本：`scripts/ux_walkthrough.py`（走查）、`scripts/build_ux_report.py`（截图 base64 内嵌，支持 argv 指定模板/输出）。

## 2. 关键结论

- **推翻 3 处**：三步走常驻引导 / 复制升主按钮+推送降级 / 92 套主题人工打标签分类。
- **修正 4 处归因**：`<br>` 是前端预览卡 bug（非成品封面）；溢出根因在引擎模板写死字号（非前端 CSS）；慢的大头是联网搜图 15–20s；「AI 自动提取标题」是假文案（实为本地函数）。
- **最终清单**：必修 6 · 应修 5 · 可选 4 · **明确不做 4**（写在方案里防止以后捡回来）。
- **开源落地**：fitty（治溢出，本周可上）／md-wechat 悬停试看 ／doocs/md 范式 ／satori 中期去浏览器化；进度反馈、搜图缓存、记住主题、署名如实——自研。

## 3. 验收指标（实施阶段用）

| 指标 | 现在 | 目标 |
|------|------|------|
| 封面成品一次合格率 | 有溢出/重叠/`<br>` | **100% 无溢出、无重叠、无 `<br>`** |
| 打开 → 选好常用主题 | 扫 92 个色卡 | **≤ 2 步** |
| 文章 → 复制/推送完成 | 6 步以上 | **≤ 3 步、0 卡点** |
| 生成等待"以为卡住" | 有 | **0 次**（点后 100ms 内出进度） |
| 生成端到端耗时 | 十几秒 | 先测基线；第二次起（缓存）明显下降 |
| 降级是否说清原因 | 署名写死 | **每次降级都写原因** |

## 4. 文档同步（本阶段已完成）

- `files/TODO.md`：已完成区加 `UX-DESIGN`；待办区立项 U-1…U-15（分三批）+「明确不做」清单。
- `AGENTS.md`：新增 §12「前端改造方案 v3」——权威文档指针、三条设计铁律、审计实锤（防重复误判）、不做清单、实施纪律。
- `HANDOFF.md`：新增「变更 11」（本轮内容，标注未动代码与下一步）。
- 本文件：`docs/task_closure_ux-design.md`（阶段收尾）。

## 5. 下一步（待批准）

按 `final-plan.html`「第一批（止血）」顺序实施 U-1 → U-6，纪律：
一次一项 → 改完即测（`test_e2e` 52/52 + `test_integration` 35/35，UI 改动加 `test_headed_full_e2e` 32/32，封面实跑三引擎出真实 PNG）→ 单项 commit → 回 TODO 打勾。

全部完成后：写实施阶段 `task_closure_*.md` + 文档同步 + git 归档；**push 需用户明确指令**（项目纪律：禁止擅自 push）。

---

*收尾状态：方案阶段已闭环（文档 + 立项 + 归档）；实施未开始，等用户批准。*
